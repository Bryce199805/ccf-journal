# 正式数据抓取执行计划（低级 Agent）

## 1. 前置条件

本任务只能在“抓取脚本构建与测试计划”已完成、用户已确认脚本冻结版本后开始。执行 Agent 不负责设计或修改爬虫，只负责按命令运行、观察、恢复、校验和汇报。

开始前必须获得构建 Agent 的交接清单，并核对：

- 当前分支及 commit SHA 与冻结版本一致；
- `npm test` 已通过；
- 发现、详情抓取和校验命令明确；
- 运行输出路径明确；
- 当前有效 `data/letpub_full.json` 可读取。

## 2. 权限边界

允许：

- 运行已冻结的抓取与校验命令；
- 创建运行时进度、暂存数据和报告；
- 安全终止并按既有机制恢复任务；
- 汇总数量、失败项和冲突项。

禁止：

- 修改任何 `.js`、测试、规则或 schema；
- 修改 `server/**`、`web/**`、`deploy/**`；
- 修改或导入生产数据库；
- 为了“跑通”而降低准入条件、跳过校验或明显缩短请求间隔；
- 手工把失败项标为成功；
- 未经校验直接覆盖正式数据；
- 提交或推送，除非用户明确授权。

如果命令、页面解析或校验程序存在缺陷，停止受影响步骤，保存现场并反馈构建 Agent，不能自行修改代码。

## 3. 数据保护与运行前记录

执行：

```bash
git status --short --branch
git rev-parse HEAD
cd scraper
npm test
```

记录正式数据在运行前的：

- 文件路径和更新时间；
- SHA-256；
- JSON 条目数；
- CCF 条目数、Non-CCF 条目数；
- 空 `journalid` 和重复 `journalid` 数。

程序应自行使用暂存文件和原子替换。若构建 Agent 未提供此保护机制，不得开始全量抓取。

## 4. 第一步：候选发现 canary

只抓取前两页：

```bash
cd scraper
DISCOVERY_MAX_PAGES=2 npm run discover:non-ccf
```

检查：

- 命令退出码为 0；
- 页面数、候选数和报告一致；
- `journalid` 无重复；
- 每条候选有来源页或 URL；
- 没有限流页、验证码页或异常短页面被当成正常结果。

任一检查失败则停止，不执行后续步骤。

## 5. 第二步：详情抓取 canary

使用 canary 候选限制为最多 10 本：

```bash
MAX_JOURNALS=10 npm run scrape
npm run validate:data
```

检查：

- 请求间隔符合构建说明；
- 成功、拒绝、失败、待处理数量闭合；
- 拒绝项带实际大类、分区和原因；
- 计算机科学 3/4区或其他大类没有进入正式结果；
- CCF 不会因分区规则被移除；
- canary 未破坏原正式数据。

任一检查失败则停止并报告。

## 6. 第三步：完整候选发现

canary 全部通过后执行：

```bash
npm run discover:non-ccf
```

要求：

- 使用 tmux、screen 或等效的持久会话；
- 不调高并发，不把默认间隔调到不合理的低值；
- 每完成约 10 页记录一次成功页、失败页、候选数和限流情况；
- 网络中断时使用已有进度恢复，不删除进度文件重跑；
- 完成后确认报告中的总页数与页面发现的总页数一致；
- 候选约 802 本只是历史估计，不要求精确相等。

如连续出现限流、验证码或页面结构异常，安全停止并反馈，不反复快速重试。

## 7. 第四步：完整详情抓取

执行冻结版本提供的全量命令：

```bash
npm run scrape
```

要求：

- 以完整候选文件为输入；
- 保持默认限速和重试参数；
- 定期记录 `pending`、`success`、`rejected`、`not_found`、`parse_failed`、`rate_limited` 数量；
- 进程中断后从进度恢复；
- 单本失败不允许清空旧数据；
- 不因预计“新增约 88 本”而手工增删条目。

## 8. 第五步：最终校验与发布

执行：

```bash
npm run validate:data
```

只有退出码为 0 且以下条件全部满足，才接受程序对 `data/letpub_full.json` 的原子发布：

- 所有正式 Non-CCF 均满足最新中科院计算机科学大类 1/2区；
- CCF 期刊完整保留；
- `journalid` 无重复；
- ISSN/EISSN 冲突均进入报告；
- 多领域 CCF 关系未丢失；
- 数量与抓取报告一致；
- 与运行前相比没有无法解释的异常下降；
- 正式 JSON 可重新读取。

发布后重新计算正式文件 SHA-256，并与运行前值一起写入报告。

## 9. 失败处理矩阵

| 情况 | 执行 Agent 动作 |
|---|---|
| 临时网络超时 | 让内置重试处理；达到上限后保留进度并停止 |
| LetPub 限流或验证码 | 停止运行，记录时间、URL 和响应特征 |
| 页面结构无法解析 | 记录 `journalid` 和脱敏样例，退回构建 Agent |
| 校验失败 | 不发布，保留暂存文件和报告 |
| 正式数量异常下降 | 不发布，报告新旧统计差异 |
| 脚本崩溃 | 保存日志和进度，按交接说明恢复；重复失败则停止 |
| 身份冲突 | 保留冲突报告，不人工猜测合并 |

## 10. 完成报告

执行 Agent 最终只需报告运行和数据结果，至少包含：

1. 冻结版本的分支和 commit SHA；
2. 每条实际执行命令、开始/结束时间和退出码；
3. 候选总页数和候选总数；
4. 详情抓取各状态计数；
5. 最终 CCF 数、Non-CCF 数、1区数、2区数和净新增数；
6. `not_found`、`parse_failed`、`rate_limited` 和身份冲突清单；
7. 校验结果；
8. 正式文件更新前后的 SHA-256；
9. 是否发生过中断及恢复次数；
10. `git status --short`；
11. 明确声明未修改抓取代码、用户系统和生产数据库。

执行任务完成不代表可以提交或上线；后续导入数据库、用户系统和云部署应作为独立任务处理。
