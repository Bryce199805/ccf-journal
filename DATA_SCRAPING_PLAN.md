# 期刊数据抓取任务总计划

本任务拆成两个严格串行、可分别派发的子任务：

1. [抓取脚本构建与测试计划](DATA_SCRAPER_BUILD_PLAN.md)：交给高级 Agent，负责实现、修正和离线测试抓取工具，不执行正式抓取。
2. [正式数据抓取执行计划](DATA_SCRAPING_RUN_PLAN.md)：交给低级 Agent，只使用已经验收并冻结的脚本执行真实抓取、恢复、校验和报告，不修改业务逻辑。

必须先完成任务 1，并由用户确认其交接清单，才能开始任务 2。如果任务 2 发现脚本缺陷，应停止受影响步骤并反馈，不得自行修补后继续。

## 共同目标

最终目录包括：

- `data/all_journals_correct.json` 中的全部 CCF 期刊，不受中科院分区限制；
- Non-CCF 中最新可用中科院分区的“大类学科”严格等于 `计算机科学`，且“大类分区”为 `1区` 或 `2区` 的期刊。

预计净新增约 88 本仅供评估运行规模，不得写入断言或作为验收条件。最终数量以实际抓取和校验结果为准。

## 共同边界

- 当前工作分支：`feat/journal-catalog-update`。
- 只处理候选发现、详情抓取、准入规则、数据校验和抓取报告。
- 不修改 `server/**`、`web/**`、用户系统、云部署或生产数据库。
- `deploy/` 是原有未跟踪目录，不得删除、覆盖或提交。
- 工作区已有其他未提交改动，禁止使用 `git reset --hard`、`git checkout --` 或批量清理工作区。
- 未经用户明确要求，不提交、不推送。

## 任务交接顺序

```text
高级 Agent 构建脚本
        ↓
离线测试全部通过 + 生成交接清单
        ↓
用户确认代码冻结版本
        ↓
低级 Agent 执行 canary
        ↓
canary 校验通过
        ↓
完整发现与详情抓取
        ↓
最终校验、原子发布和运行报告
```

两个子任务的命令、权限、验收标准和异常处理以各自文档为准。
